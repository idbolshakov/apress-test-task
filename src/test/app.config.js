import assert from 'assert';

import appConfig from '../main/appConfig.es6.js';


//
// appConfig object
//
describe('appConfig object', () => {

    it('Should be an object', () => {

        assert.equal('object', typeof appConfig);
    });

   // 
   // structure
   //
   describe('config structure', () => {
   });
});
